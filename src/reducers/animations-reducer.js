import { TRIGGER_ANIMATION } from '../actions/types';

const initialState = {
    start:0
}

const rootReducer = ( state = initialState, action) => {
    if( action.type === TRIGGERANIMATION )  {
           
    }
}